# --- IGNORE EVERYTHING ---
# We ignore the massive data folders so the Root Git doesn't scan them
01_Projects/
02_Exports/
03_Vault/
04_Global_Assets/
E_Drive_Links/

# --- IGNORE SYSTEM JUNK ---
.DS_Store
Thumbs.db
*.log
*.tmp

# --- ALLOWLIST (Only track these) ---
# We explicitly allow the System folder because that is what you want to back up
!00_System/
!README.md
!.gitignore
