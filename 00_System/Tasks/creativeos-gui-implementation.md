# 🎯 Task: Lightweight CreativeOS GUI (FastAPI + Vite)

**Objective:** Build a lightweight, PWA-installable web-based GUI for CreativeOS that wraps the existing Python CLI logic.
**Priority:** High
**Scope:** Included: Dashboard, Project Cards, Storage Overview, New Project Form, Settings view. Excluded: File explorer/browsing, editing metadata, archive/resurrect operations.

---

## 📋 Requirements

### Functional Requirements
- **[REQ-001]** Dashboard showing project cards with name, category icon, created date, and active/stale status.
- **[REQ-002]** Storage Overview table showing sortable project storage data (total, media, reclaimable).
- **[REQ-003]** New Project form to create projects via GUI (using existing `cos new` logic).
- **[REQ-004]** `cos gui` command to launch the local FastAPI server and open the default browser.
- **[REQ-005]** PWA support (manifest + basic service worker) so users can "Install" the app from the browser.

### Technical Requirements
- **[TECH-001]** Backend: FastAPI + uvicorn serving as a thin API wrapper over existing `cos` Python modules.
- **[TECH-002]** Frontend: Vanilla HTML/CSS/JS bundled with Vite (no React/Vue needed unless absolutely required for state management).
- **[TECH-003]** Minimal dependencies: Only add `fastapi` and `uvicorn` to Python requirements. No Electron.
- **[TECH-004]** Design: Clean UI, Light/Dark mode toggle. Neutral palette (grays, whites, blacks, subtle blue accents). No purple gradients.

---

## 🏗️ Implementation Plan

### Phase 1: Backend API Wrapper
- [x] Add `fastapi>=0.100.0` and `uvicorn[standard]>=0.20.0` to `requirements.txt`
- [x] Create `00_System/Scripts/cos/api.py` with FastAPI endpoints (`/api/projects`, `/api/storage`, `/api/config`, `/api/sync`, `/api/categories`) mapping to existing `cos` functions.
- [x] Create `00_System/Scripts/cos/commands/gui.py` to start uvicorn and open the browser.
- [x] Update `00_System/Scripts/cos/cli.py` to register the new `gui` command.

### Phase 2: Frontend Setup & UI Tokens
- [x] Initialize Vite project in `00_System/GUI` (`npx -y create-vite@latest ./ --template vanilla`)
- [x] Create CSS variables in `tokens.css` for Light and Dark modes.
- [x] Setup proxy in `vite.config.js` to point `/api` to `localhost:8787` for development.

### Phase 3: Core Views Implementation
- [x] Build `index.html` (App Shell & Navigation).
- [x] Implement Dashboard view (`dashboard.js` & `project-card.js`).
- [x] Implement Storage Review view (`storage.js` & `table.js`).
- [x] Implement New Project form (`new-project.js`).
- [x] Implement Settings view (`settings.js`).

### Phase 4: PWA & Integration
- [x] Add `public/manifest.json` and a basic `public/sw.js` for PWA installation.
- [x] Run `npm run build` in `00_System/GUI` to generate the `dist` folder.
- [x] Update `api.py` to mount and serve the built static files from `00_System/GUI/dist/` at the root `/` path.
- [x] Test the full `cos gui` flow (backend + frontend).

---

## 📁 Files to Create/Modify

| File | Action | Purpose |
|------|--------|---------|
| `requirements.txt` | Modify | Add fastapi and uvicorn |
| `00_System/Scripts/cos/api.py` | Create | FastAPI wrapper server |
| `00_System/Scripts/cos/commands/gui.py` | Create | CLI command to launch server |
| `00_System/Scripts/cos/cli.py` | Modify | Register `gui` subcommand |
| `00_System/GUI/` | Create | Vite frontend project |
| `00_System/GUI/public/manifest.json` | Create | PWA definition |

---

## ✅ Success Criteria

### Code Quality
- [x] Backend avoids duplicating logic (uses existing `cos` modules).
- [x] Frontend uses clean, modular JS structure.
- [x] No unnecessary dependencies introduced.

### Performance
- [x] FastAPI server starts in under 1 second.
- [x] Frontend is extremely lightweight and fast to render.

### Functionality
- [x] Running `cos gui` successfully opens the dashboard in the browser.
- [x] Data correctly populates from the file system.
- [x] Creating a project via GUI works end-to-end.
- [x] Dark mode toggle works seamlessly.
- [x] App is installable via Chrome PWA feature.

---

## 🔗 Dependencies

**Related files for context:**
- `00_System/Scripts/cos/cli.py`
- `00_System/Scripts/cos/config.py`
- `00_System/Scripts/cos/storage.py`
- `00_System/Scripts/cos/commands/new.py`

---

## 🚀 Getting Started

1. Read this task prompt completely.
2. Review the related Python files listed above to understand the current logic you'll be wrapping.
3. Begin with Phase 1: Backend API Wrapper.
4. Provide progress updates after each phase.

---

*Generated by /vibe-spawnTask workflow*
