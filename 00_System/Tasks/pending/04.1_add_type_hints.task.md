# Task: Add Type Hints to Core Functions

**Session ID:** cos-prod-readiness-2026  
**Task ID:** 04.1  
**Priority:** P1 - HIGH  
**Category:** CODE QUALITY  
**Assigned To:** vibe-code  
**Dependencies:** Phase 1, Phase 2, Phase 3

---

## 📋 Objective

Add Python type hints to all functions in `manage.py` for better IDE support and static type checking.

## 🎯 Scope

**In Scope:**
- Add type hints to all function signatures
- Add type aliases for complex types
- Add `from typing import ...` imports

**Out of Scope:**
- Changes to function logic
- Changes to other files

---

## 📚 Context

### Code Quality Issue
The entire codebase is untyped. No type hints anywhere, which means:
- No IDE autocomplete support
- No static type checking
- Runtime errors that could be caught at development time

---

## ✅ Definition of Done

- [ ] `typing` imports added
- [ ] Type aliases defined
- [ ] All functions have type hints
- [ ] Functions return types specified
- [ ] Optional parameters marked as `Optional`

---

## 📁 Expected Artifacts

| File | Action | Notes |
|------|--------|-------|
| `00_System/Scripts/manage.py` | Modified | Add type hints throughout |

---

## 📝 Implementation Code

### Step 1: Add typing imports (at top of file after other imports)

```python
from typing import Optional, Dict, List, Tuple, Any, Union
from pathlib import Path
```

### Step 2: Add type aliases (after imports, before constants)

```python
# Type aliases
JSONDict = Dict[str, Any]
FileFingerprint = Dict[str, Union[float, int]]
SyncState = Dict[str, Any]
```

### Step 3: Add type hints to key functions

```python
def get_date_slug(override_date: Optional[str] = None) -> str:
    """Generate a date slug for project naming."""
    if override_date:
        return override_date
    return datetime.datetime.now().strftime("%Y-%m-%d")


def format_path(path: str) -> str:
    """Returns a clickable rich string for the given path."""
    try:
        abs_path: str = os.path.abspath(path)
        url: str = urllib.request.pathname2url(abs_path)
        return f"[link=file:{url}][path]{path}[/path][/link]"
    except Exception:
        return f"[path]{path}[/path]"


def get_export_month_path() -> str:
    """Get the current month's export path."""
    now = datetime.datetime.now()
    year: str = now.strftime("%Y")
    month_name: str = now.strftime("%B")
    month_num: str = now.strftime("%m")
    
    full_path: str = os.path.join(EXPORTS_PATH, year, f"{month_num} - {month_name}")
    if not os.path.exists(full_path):
        os.makedirs(full_path)
    return full_path


def find_meta_in_cwd() -> Tuple[Optional[JSONDict], Optional[str]]:
    """Search upward from current directory for .project_meta.json."""
    # ... existing implementation


def get_smart_date(path: str) -> float:
    """Calculate the median timestamp of files in a directory."""
    # ... existing implementation


def load_sync_state() -> SyncState:
    """Load the sync state database for incremental syncs."""
    # ... existing implementation


def save_sync_state(state: SyncState) -> None:
    """Save the sync state database."""
    # ... existing implementation


def get_file_fingerprint(filepath: str) -> Optional[FileFingerprint]:
    """Get a fast fingerprint for a file using mtime and size."""
    # ... existing implementation


def get_syncable_files(root_dir: str) -> Dict[str, Optional[FileFingerprint]]:
    """Recursively find all .md and .pdf files."""
    # ... existing implementation


def sync_two_folders(
    dir_a: str, 
    dir_b: str, 
    prev_state: Optional[SyncState] = None
) -> Tuple[List[Dict[str, str]], SyncState]:
    """Bidirectional Sync: A (Project) <-> B (Vault)."""
    # ... existing implementation


def copy_with_progress(src: str, dst: str) -> None:
    """Copies files from src to dst, showing a rich progress bar."""
    # ... existing implementation


def setup_git(project_path: str, category: str) -> None:
    """Initializes Git and adds .gitignore."""
    # ... existing implementation


def sanitize_path_input(value: str, max_length: int = 100) -> str:
    """Sanitize user input to prevent path traversal attacks."""
    # ... existing implementation


def validate_git_url(url: str) -> str:
    """Validate and sanitize a Git repository URL."""
    # ... existing implementation
```

---

## 🚫 Constraints

- ONLY add type hints, do not change logic
- Use `Optional` for optional parameters
- Use `Union` where multiple types are possible
- Add return types to all functions

---

## 🧪 Verification Steps

1. **Run mypy:**
   ```bash
   mypy 00_System/Scripts/manage.py --ignore-missing-imports
   ```
   Expected: Fewer errors than before (some may remain due to external libraries)

2. **Check IDE support:**
   - Open `manage.py` in VS Code
   - Hover over function calls
   - Expected: Type information displayed

---

*Generated by vibe-orchestrator mode*