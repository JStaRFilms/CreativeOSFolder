# Task: Decouple Categories from Code (Easy Expansion)

**Task ID:** 09.1
**Priority:** P2 - MEDIUM
**Category:** REFACTOR
**Assigned To:** vibe-code

---

## 📋 Objective

Currently, adding a new project category requires modifying `cos/commands/new.py` to update mapping and validation. This task refactors the system to read categories and their mappings from a configuration file, allowing users to add new categories just by editing JSON.

---

## 🎯 Scope

- Create a `categories.json` in `00_System/Config/`.
- Refactor `cos/commands/new.py` to load choices and template mappings from this JSON.
- Ensure the CLI help text dynamically updates to show valid categories.
- Support a default fallback category if none matches.

---

## ✅ Definition of Done

- [ ] `00_System/Config/categories.json` created with all existing mappings.
- [ ] `new.py` no longer has hardcoded category strings in `choices` or mapping logic.
- [ ] Adding a new entry to the JSON automatically makes it a valid CLI choice.
- [ ] Verified by adding a dummy "Test" category via JSON and creating a project with it.

---

*Generated by vibe-code mode | 2026-02-25*
