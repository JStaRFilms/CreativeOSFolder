# Task: Add Logging Framework

**Session ID:** cos-prod-readiness-2026  
**Task ID:** 04.4  
**Priority:** P1 - LOW  
**Category:** CODE QUALITY  
**Assigned To:** vibe-code  
**Dependencies:** None

---

## 📋 Objective

Add Python logging for debugging and operational visibility.

## 🎯 Scope

**In Scope:**
- Add logging configuration
- Add logger to key functions
- Log to file in Config directory

**Out of Scope:**
- Changes to console output (keep using Rich)
- Changes to error handling

---

## 📚 Context

### Code Quality Issue
No logging framework exists. All output goes to console via Rich. For debugging and operational monitoring, a persistent log is needed.

---

## ✅ Definition of Done

- [ ] `logging` module imported
- [ ] Logger configured
- [ ] Log file created in Config directory
- [ ] Key operations logged (project creation, sync, errors)
- [ ] Log rotation considered

---

## 📁 Expected Artifacts

| File | Action | Notes |
|------|--------|-------|
| `00_System/Scripts/manage.py` | Modified | Add logging |

---

## 📝 Implementation Code

### Step 1: Add import at top of file

```python
import logging
```

### Step 2: Add logging configuration (after constants, before functions)

```python
# --- LOGGING SETUP ---
LOG_PATH = os.path.join(SCRIPT_DIR, "..", "Config", "creativeos.log")

# Create logger
logger = logging.getLogger('creativeos')
logger.setLevel(logging.DEBUG)

# Create file handler
file_handler = logging.FileHandler(LOG_PATH, encoding='utf-8')
file_handler.setLevel(logging.DEBUG)

# Create formatter
formatter = logging.Formatter(
    '%(asctime)s - %(levelname)s - %(message)s',
    datefmt='%Y-%m-%d %H:%M:%S'
)
file_handler.setFormatter(formatter)

# Add handler to logger
logger.addHandler(file_handler)
```

### Step 3: Add logging to key functions

```python
def cmd_new(args):
    """Create a new project."""
    logger.info(f"Creating new project: {args.name} (category: {args.category})")
    # ... existing code ...
    logger.debug(f"Project created at: {target_dir}")


def cmd_sync(args):
    """Sync Notes between Projects and Obsidian Vault."""
    logger.info("Starting sync operation")
    # ... existing code ...
    logger.info(f"Sync complete: {total_changes} operations across {projects_synced} projects")


def cmd_clone(args):
    """Clones a Git repo and adopts it into CreativeOS."""
    logger.info(f"Cloning repository: {args.url}")
    # ... existing code ...
    logger.debug(f"Clone complete: {target_dir}")
```

---

## 🚫 Constraints

- ONLY add logging, do not change logic
- Keep console output using Rich (logging is for file only)
- Do not log sensitive information

---

## 🧪 Verification Steps

1. **Check log file created:**
   ```bash
   cat 00_System/Config/creativeos.log
   ```
   Expected: Log entries visible

2. **Run a command and check log:**
   ```bash
   cos new "Test Project" -c Video
   cat 00_System/Config/creativeos.log
   ```
   Expected: New log entry for project creation

---

*Generated by vibe-orchestrator mode*