# Task: Add Path Validation for Metadata Discovery

**Session ID:** cos-prod-readiness-2026  
**Task ID:** 01.4  
**Priority:** P0 - HIGH  
**Category:** SECURITY  
**Assigned To:** vibe-code  
**Dependencies:** None

---

## 📋 Objective

Validate that discovered `.project_meta.json` files are within expected project boundaries and have valid structure.

## 🎯 Scope

**In Scope:**
- Update `find_meta_in_cwd()` to validate metadata structure
- Add path boundary validation
- Improve error handling

**Out of Scope:**
- Changes to other functions
- Changes to file structure

---

## 📚 Context

### Security Issue (SEC-006)
`find_meta_in_cwd()` traverses up the directory tree and reads `.project_meta.json` without validating the path. A malicious `.project_meta.json` placed higher in the directory tree could be picked up.

### Affected Code Location
- `00_System/Scripts/manage.py:96-106`

---

## ✅ Definition of Done

- [x] Metadata structure validation added (required fields check)
- [x] Path boundary validation added (must be within PROJECTS_PATH)
- [x] Specific exception handling (no bare `except:`)
- [x] User-friendly warning messages
- [x] Cross-platform root detection

---

## 📁 Expected Artifacts

| File | Action | Notes |
|------|--------|-------|
| `00_System/Scripts/manage.py` | Modified | Update `find_meta_in_cwd()` function |

---

## 📝 Implementation Code

Replace the entire `find_meta_in_cwd()` function (lines 96-106):

```python
def find_meta_in_cwd():
    """
    Search upward from current directory for .project_meta.json.
    
    Performs a 3-level upward search for project metadata.
    Validates that the metadata file is within expected bounds.
    
    Returns:
        tuple: (metadata_dict, project_root) or (None, None) if not found
    """
    current = os.getcwd()
    
    for _ in range(3):
        meta_path = os.path.join(current, ".project_meta.json")
        
        if os.path.exists(meta_path):
            try:
                with open(meta_path, "r", encoding="utf-8-sig") as f:
                    meta = json.load(f)
                
                # Validate metadata structure
                required_fields = {"name", "slug", "type", "created"}
                if not required_fields.issubset(meta.keys()):
                    console.print(f"[warning]⚠️  Found metadata but missing required fields: {meta_path}[/warning]")
                    return None, None
                
                # Validate path is within PROJECTS_PATH
                try:
                    abs_current = os.path.realpath(current)
                    abs_projects = os.path.realpath(PROJECTS_PATH)
                    
                    if not abs_current.startswith(abs_projects):
                        console.print(f"[warning]⚠️  Found metadata outside projects path: {meta_path}[/warning]")
                        return None, None
                except Exception:
                    pass  # If path validation fails, continue anyway
                
                return meta, current
                
            except json.JSONDecodeError as e:
                console.print(f"[warning]⚠️  Invalid JSON in metadata: {meta_path}[/warning]")
                return None, None
            except IOError as e:
                console.print(f"[warning]⚠️  Cannot read metadata: {e}[/warning]")
                return None, None
        
        # Move up one directory
        parent = os.path.dirname(current)
        
        # Check if we've reached the root (cross-platform)
        if parent == current:
            break
            
        current = parent
    
    return None, None
```

---

## 🚫 Constraints

- ONLY modify the `find_meta_in_cwd()` function
- Preserve the function signature
- Keep the 3-level traversal limit

---

## 🧪 Verification Steps

1. **Test from within valid project:**
   - Navigate to a project with valid `.project_meta.json`
   - Run a context-aware command
   - Expected: Metadata found and used

2. **Test from outside PROJECTS_PATH:**
   - Navigate to a directory outside projects
   - Place a `.project_meta.json` there
   - Run a context-aware command
   - Expected: Metadata ignored with warning

3. **Test with corrupted metadata:**
   - Create a project with invalid JSON in `.project_meta.json`
   - Run a context-aware command
   - Expected: Warning about invalid JSON

4. **Test with missing fields:**
   - Create a project with `.project_meta.json` missing required fields
   - Run a context-aware command
   - Expected: Warning about missing fields

---

*Generated by vibe-orchestrator mode*