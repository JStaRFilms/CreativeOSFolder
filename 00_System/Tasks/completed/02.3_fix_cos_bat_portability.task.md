# Task: Fix cos.bat Portability

**Session ID:** cos-prod-readiness-2026  
**Task ID:** 02.3  
**Priority:** P0 - HIGH  
**Category:** PORTABILITY  
**Assigned To:** vibe-code  
**Dependencies:** None

---

## 📋 Objective

Fix `cos.bat` to use relative paths instead of hardcoded absolute path, making CreativeOS portable.

## 🎯 Scope

**In Scope:**
- Update `cos.bat` to use `%~dp0` for script directory

**Out of Scope:**
- Changes to `manage.py`
- Changes to other files

---

## 📚 Context

### Portability Issue (LOG-006)
`cos.bat` uses hardcoded absolute path `C:\CreativeOS\00_System\Scripts\manage.py`, making the CLI non-portable to other locations or drives.

### Affected Code Location
- `00_System/Scripts/cos.bat:1`

---

## ✅ Definition of Done

- [ ] `cos.bat` uses `%~dp0` for relative path
- [ ] CLI works from any installation location
- [ ] Works on any drive letter

---

## 📁 Expected Artifacts

| File | Action | Notes |
|------|--------|-------|
| `00_System/Scripts/cos.bat` | Modified | Use relative path |

---

## 📝 Implementation Code

Replace the entire contents of `cos.bat`:

```batch
@echo off
REM CreativeOS CLI Entry Point
REM Uses %~dp0 to get the script's directory for portability

python "%~dp0manage.py" %*
```

---

## 🚫 Constraints

- ONLY modify `cos.bat`
- Keep the file minimal

---

## 🧪 Verification Steps

1. **Test from current location:**
   - Run `cos` from command line
   - Expected: Works correctly

2. **Test after moving CreativeOS:**
   - Move entire CreativeOS folder to different drive (e.g., D:\)
   - Update config.json paths
   - Run `cos`
   - Expected: Works correctly from new location

---

*Generated by vibe-orchestrator mode*